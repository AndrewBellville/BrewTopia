<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/ColorToneM1</item>
        <item name="colorPrimaryDark">@color/ColorToneD1</item>
        <item name="colorAccent">@color/ColorToneL1</item>
    </style>

    <style name="DialogStyle" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@color/transparent_gray</item>
    </style>

</resources>
